def count_the_vowels(word):
    count=0
    list_1=["a","e","i","o","u"]
    for char in word:
        if char in list_1:
            count=count+1 
    return count
word = input()
vowels=count_the_vowels(word)
print(vowels)
